# Ansible Inventory File
# Configure your target hosts here

# ==============================================================================
# QUICK START
# ==============================================================================
# 1. Uncomment and edit [host_machine] section with your host IP
# 2. Choose SSH key or password authentication
# 3. Refresh web interface - new targets appear in dropdown
#
# For more examples, see: inventory/hosts.example
# For setup guide, see: docs/CONFIGURATION.md

# ==============================================================================
# LOCAL (Container localhost)
# ==============================================================================
[local]
# Uncomment to run playbooks against container itself
# localhost ansible_connection=local

# ==============================================================================
# HOST MACHINE (Docker host)
# ==============================================================================
[host_machine]
192.168.1.50 ansible_user=svc-ansible ansible_ssh_private_key_file=/app/.ssh/svc-ansible-key

# ==============================================================================
# REMOTE SERVERS
# ==============================================================================
[remote_servers]
# Add your remote servers here
# Example:
# server1.example.com ansible_user=admin ansible_ssh_private_key_file=/app/.ssh/id_rsa
# 192.168.1.50 ansible_user=admin ansible_ssh_pass=password

# ==============================================================================
# GROUP COMPOSITION
# ==============================================================================
[all_hosts:children]
host_machine
remote_servers

# ==============================================================================
# NOTES
# ==============================================================================
# - SSH keys should be placed in .ssh/ directory (auto-excluded from git)
# - For service account setup, run: sudo bash setup-svc-ansible.sh
# - All groups/hosts automatically appear in web interface dropdown
# - Changes take effect immediately (no restart needed)
# - See docs/CONFIGURATION.md for advanced configuration options
