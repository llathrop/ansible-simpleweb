# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
ENV/
*.egg-info/
.eggs/

# Flask
instance/
.webassets-cache

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# SSH Keys and Sensitive files
*.key
*.pem
id_rsa*
.ssh/

# Project context and prompts (local/session use; may contain internal notes)
memory.md
Next-prompt.md

# IDE / agent config (local)
.claude
.cursor/

# Inventory files with credentials (keep sample, ignore local configs)
inventory/hosts
inventory/hosts.local
inventory/managed_hosts.ini
inventory/routers
inventory/*.local
inventory/group_vars/
!inventory/hosts.sample

# Logs (execution logs - can be large; may contain local hostnames/IPs)
logs/
!logs/.gitkeep

# Runtime configuration (user-specific)
# Contains schedules, history, inventory data, host_facts
config/

# Temporary files and local backups (do not commit)
/tmp/
*.tmp
*.bak
ansible-simpleweb-backup-*.tar
validation_backup*/
*_backup_*.yaml
*_backup_*.yml

# MongoDB data (if running locally outside Docker)
data/db/

# Local development overrides
docker-compose.override.yml
.env
.env.local

# Test output
*.test.json
test_output/
